extends layout

block content

    h1 Message Board

    ul.messages
        each message in messages
            li.message
                a(href=message.user.url) #{message.user.username}:
                p.message-text #{message.text}

    unless messages.length
        p No messages have been sent, be the first!